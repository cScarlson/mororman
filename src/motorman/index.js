
var { Motorman } = require('./motorman');
var { DelegatesManager } = require('./delegates-manager');
var { DelegationsManager } = require('./delegations-manager');

module.exports = {
    maps: require('./maps'),
    Motorman,
    DelegatesManager,
    DelegationsManager,
};
